# NF-V2-03 Formal Attempt 3

Transport-only retry policy was sealed before this run. All 72 predictions
were sealed before Gold scoring. Eight questions had no fact supply and were
deterministically skipped; 64 provider calls returned structured JSON. The
post-seal binding audit found slot-ID/selection failures and two false-binding
queries, so the strong-pass safety gate failed. Production remains V1.
