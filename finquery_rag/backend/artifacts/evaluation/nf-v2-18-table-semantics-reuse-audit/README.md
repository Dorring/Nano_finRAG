# NF-V2-18A-R2P0

This directory records the audit-only decision for reusing the existing financial-table semantic stack. See final-reuse-audit.md and decision.json.

No benchmark was rerun, no retrieval or parser behavior was changed, and no model calls were made. The recommended next step is an implementation task for one HTML physical adapter followed by adapter-level tests; do not create parallel FinancialColumnContextV1 or FinancialCellEvidenceV1 abstractions.

